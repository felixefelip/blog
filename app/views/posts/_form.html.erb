<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :title, class:"form-control mb-3", placeholder:"Título *" %>

  <%= form.text_area :content, class:"form-control mb-3", placeholder:"Texto *" %>


  <%= form.number_field :user_id, user_id: :user_id, value:current_user.id, type: :hidden %>

  <small class="form-text text-muted d-block">(*) Campos obrigatórios</small>

  <%= form.submit class:"btn btn-primary mt-2"  %>
<% end %>
